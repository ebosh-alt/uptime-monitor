---

## 🧪 Тестовое задание: **"Сервис проверки доступности веб-сайтов (Async Uptime Monitor)"**

---

### 📌 Цель:

Разработать сервис, который **периодически пингует заданные URL**, сохраняет результаты в хранилище (in-memory или БД),
и предоставляет REST API:

* Добавить URL
* Удалить URL
* Получить историю проверок
* Получить статус всех URL

---

### 🧠 Что надо реализовать:

#### 1. **Фоновый пингер**

* Каждый URL проверяется раз в `N` секунд
* Не более `K` URL проверяются одновременно (использовать семафор)
* Каждая проверка выполняется в горутине с таймаутом
* Поддержка graceful shutdown

#### 2. **REST API**

* `POST /url` — добавить URL
* `DELETE /url/{id}` — удалить URL
* `GET /url` — получить список URL
* `GET /url/{id}/history` — получить последние проверки (с таймстампом, статусом)

#### 3. **Хранилище**

* In-memory (map + mutex) или PostgreSQL
* Сохранять результаты в виде: `url, status, latency, timestamp`

#### 4. **Конфиг**

* Период опроса (`N` секунд)
* Лимит одновременных проверок (`K`)
* Таймаут запроса

#### 5. **Логирование**

* В формате JSON через `zap`, `zerolog`, либо стандартное `log`

#### 6. **Тесты**

* Unit тесты для хранилища, API, и воркеров
* (Бонус) — интеграционные тесты

---

### ⚙️ Технические требования:

* Go 1.20+
* Работа с каналами, семафором, waitgroup
* Чистая архитектура (usecase/domain/repo/delivery)
* REST через `net/http` или `echo`, `fiber`, `gin`
* Конфигурация через `viper` или `.env`
* Прогресс логирование/метрики (бонус)

---

### 📁 Возможная структура проекта:

```
uptime-monitor/
├── cmd/
│   └── main.go
├── internal/
│   ├── domain/
│   │   └── model.go
│   ├── usecase/
│   │   └── monitor.go
│   ├── delivery/
│   │   └── http/
│   │       └── handler.go
│   ├── repository/
│   │   └── memory/
│   │       └── storage.go
├── pkg/
│   └── logger/
├── test/
│   └── integration/
│       └── monitor_test.go
├── go.mod
└── README.md
```

---

### 🧩 Что проверяет:

| Навык                       | Как проявляется                      |
|-----------------------------|--------------------------------------|
| Архитектура                 | Понимание разделения ответственности |
| Горутинки, каналы, семафоры | Параллелизм и синхронизация          |
| HTTP                        | Обработка API-запросов               |
| Тестирование                | Юнит-тесты и структура               |
| Конфигурация и логирование  | Подготовка к продакшн среде          |

---
